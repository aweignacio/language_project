# compose.yaml 用的設定範本
#
# 使用方式：複製此檔為 .env，並填入實際值。
# .env 已列入 .gitignore，不會進入版本控制。
#
# ★ 這裡的密碼是「你自己決定的」，不是要去哪裡查出來的 ——
#   PostgreSQL 容器第一次啟動時就是拿這個值建立 postgres 使用者。
#   填好之後，application-local.yml 的 spring.datasource.password
#   也要填同一組，兩邊必須一致。

POSTGRES_PASSWORD=請填入資料庫密碼
